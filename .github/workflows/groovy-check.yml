name: Groovy Check

on:
  push:
    branches: [ main ]

jobs:
  groovy-check:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Groovy
        uses: actions/setup-java@v3
        with:
          java-version: 11
          distribution: openjdk
